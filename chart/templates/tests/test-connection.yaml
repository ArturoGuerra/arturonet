apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arturonet.fullname" . }}-test-connection"
  labels:
    {{- include "arturonet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arturonet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
